#include<iostream>
#include<bitset>
using namespace std;

int main() {
	int W,H,N;
	cin >> W >> H >> N;
	bitset<W> xs;
	bitset<H> ys;
	for (int i = 0; i < N; i++) {
		int x, y, a;
		cin >> x >> y >> a;
		if (a == 1) xs -= 
	}
}
